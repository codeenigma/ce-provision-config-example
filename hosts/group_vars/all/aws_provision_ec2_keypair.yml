---
# Default AWS key pair
# Change the key name to include your controller hostname
aws_provision_ec2_keypair:
  aws_profile: "{{ _aws_profile }}"
  region: "{{ _aws_region }}"
  key_name: controller@controller
